{% if data|length %}
    <ul>
        {% for channel in data %}
            <li>
                {{ channel.name }}
            </li>
        {% endfor %}
</ul>
{% else %}
    <i class="exclamation circle icon red"></i> {{ 'setono_sylius_facebook.ui.no_channels'|trans }}
{% endif %}

sylius_grid:
  grids:
    setono_sylius_facebook_tracking_plugin_admin_facebook_config:
      driver:
        options:
          class: "%setono_sylius_facebook_tracking_plugin.model.facebook_config.class%"
      fields:
        code:
          type: string
          label: setono_sylius_facebook_tracking_plugin.ui.code
        name:
          type: string
          label: setono_sylius_facebook_tracking_plugin.ui.name
        enabled:
          type: twig
          label: setono_sylius_facebook_tracking_plugin.ui.enabled
          options:
            template: SyliusUiBundle:Grid/Field:enabled.html.twig
      filters:
        name:
          type: string
      actions:
        main:
          create:
            type: create
        item:
          update:
            type: update
          delete:
            type: delete